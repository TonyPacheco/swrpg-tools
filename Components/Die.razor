@using static CharacterSheet.RenderExtensions

<i class="ffi @GetDieClass(Type, Outline)" style="font-size: large"></i>

@code 
{
    [Parameter, EditorRequired] public DieType Type { get; set; }
    [Parameter] public bool Outline { get; set; } = false;
}
